<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.kgw.app.board.notice.NoticeDAO">
    	<!-- 반복되는 쿼리를 모아논것 -->
    	<sql id="search">
    	
    		<if test="kind == 'k1'">
    			WHERE board_title Like concat('%', #{search} ,'%')
    		</if>
    		<if test="kind == 'k2'">
    			WHERE board_contents Like concat('%',#{search},'%')
    		</if>
    		<if test="kind == 'k3'">
    			WHERE board_writer Like concat('%',#{search},'%')
    		</if>
    		
    	</sql>
    	
    	<!-- 조회 -->
    	<select id="detail" resultMap="detailResult" parameterType="NoticeDTO">
    		select * from notice n
				left join notice_files nf
				on n.board_num = nf.board_num
			where n.board_num=#{boardNum};
    	</select>
    	
    	<resultMap type="NoticeDTO" id="detailResult">
    		<id column="board_num" property="boardNum"/>
    		<result column="board_title" property="boardTitle"/>
    		<result column="board_writer" property="boardWriter"/>
    		<result column="board_contents" property="boardContents"/>
    		<result column="board_date" property="boardDate"/>
    		<result column="board_hit" property="boardHit"/>
    		<collection property="boardFileList" javaType="java.util.List" ofType="BoardFileDTO">
				<id column="file_num" property="fileNum"/>
				<result column="file_name" property="fileName"/>
				<result column="file_origin" property="fileOrigin"/>
    		</collection>
    	</resultMap>
    	
    	
    	
    	
    	<!-- 목록 -->
    	<select id="list" resultType="NoticeDTO" parameterType="Pager">
    		SELECT * FROM notice
			<include refid="search"/>
    		ORDER BY board_num DESC
    		LIMIT #{startNum},#{perPage}
    	</select>
    	
    	<!-- 추가 -->
    	<insert id="add" parameterType="NoticeDTO" useGeneratedKeys="true" keyProperty="boardNum">
    		INSERT INTO notice 
    		(board_title, board_writer, board_contents, board_date)
    		VALUES
    		(#{boardTitle}, #{boardWriter}, #{boardContents}, CURDATE())
    	</insert>
    	
    	<!-- 수정 -->
    	<update id="update" parameterType="NoticeDTO">
    		UPDATE notice
    		SET board_title=#{boardTitle}, board_contents=#{boardContents}
    		WHERE board_num=#{boardNum}
    	</update>
    	
    	<!-- 삭제 -->
    	<delete id="delete" parameterType="NoticeDTO">
    		DELETE FROM notice
    		WHERE board_num = #{boardNum}
    	</delete>
    	
    	<select id="count" resultType="Long" parameterType="Pager">
			SELECT COUNT(*) FROM notice
			<include refid="search"/>
    	</select>
    	
    	<!-- fileAdd -->
    	<insert id="fileAdd" parameterType="BoardFileDTO">
    		INSERT INTO notice_files (file_name, file_origin, board_num)
    		VALUES (#{fileName}, #{fileOrigin}, #{boardNum})
    	</insert>
    	
    	<!-- 부모 삭제하기 전에 자식 먼저 삭제 -->
    	<delete id="deleteFile" parameterType="NoticeDTO">
    		DELETE FROM notice_files
    		WHERE board_num = #{boardNum}
    	</delete>
    	
    	<!-- file detail -->
    	<select id="fileDetail" parameterType="BoardFileDTO" resultType="BoardFileDTO">
    		SELECT * 
    		FROM notice_files
    		WHERE file_num = #{fileNum}
    	</select>
    </mapper>
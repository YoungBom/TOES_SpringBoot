<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.kgw.app.product.ProductDAO">
    	
    	<!-- 리스트 보기 -->
    	<select id="list" parameterType="ProductDTO" resultType="ProductDTO">
    		SELECT * FROM products
    	</select>
    	
    	<!-- 조회 -->
    	<select id="detail" parameterType="ProductDTO" resultType="ProductDTO">
    		SELECT * FROM products WHERE product_num=#{productNum}
    	</select>
    	
    	<!-- 추가 -->
    	<insert id="add" parameterType="productDTO">
    		INSERT INTO products 
			(product_name, product_contents, product_category, product_rate, product_sale)
			VALUES
			(#{productName} , #{productContents} , #{productCategory} , #{productRate} , #{productSale})
    	</insert>
    	
    	<!-- 삭제 -->
    	<delete id="delete" parameterType="ProductDTO">
    		DELETE FROM products
    		WHERE product_num = #{productNum}
    	</delete>
    	
    	
    	<!-- 수정 -->
    	<update id="update" parameterType="ProductDTO">
    		UPDATE products SET 
	    		product_name = #{productName},
	    		product_category = #{productCategory},
	    		product_rate = #{productRate},
	    		product_contents = #{productContents},
	    		product_sale = #{productSale}
    		WHERE product_num = #{productNum}
    		
    	</update>
    	
    	
    	
    	
    </mapper>